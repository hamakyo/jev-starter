| 出典 | 目的 | 具体対象 | 役割 | 前後関係 | 候補語 | 初出定義 |
| --- | --- | --- | --- | --- | --- | --- |
| 前回P1 cascade指摘 | primaryとfallbackを通った実際の処理量と処理結果を報告しているか判定する | fallback対象行について両providerのlatency・usage・モデル別costを合算し、primary成功・fallback移行・primary失敗を区別してresolved fractionを算出する実装とテスト | 値 | primary実行後、fallback実行後、end-to-end report生成前に集計する | cascade実行集計 | Cascade実行集計とは、各dataset行のprovider legと、primaryだけで正常完了した割合・fallbackへ移った割合・失敗した割合を区別する値を指す。 |
| 前回P2 dataset hash指摘 | fallback対象集合を再現できるか判定する | 元datasetのhash、fallback subset固有hash、選択されたID列を区別して記録するreport | 記録 | 元dataset読込後、fallback対象選択後、fallback report生成時に記録する | fallback選択記録 | Fallback選択記録とは、元datasetとそこから選択された行集合の対応を再現するhashとIDの組を指す。 |
| 前回P2 accuracy指摘 | provider failureを品質指標から隠していないか判定する | 全dataset行を分母とするaccuracyと成功predictionだけを分母とするaccuracy、およびreport表示 | 値 | 各行の成功・失敗を確定後、classification集計時に算出する | 全体正解率・成功時正解率 | 全体正解率とは失敗行を不正解として全行を分母にする値、成功時正解率とはprediction成功行だけを分母にする値を指す。 |
| 前回P1 optional correctness指摘 | reference answerがない入力をcorrectness判定へ送らず、実行した契約を再現できるか判定する | reference有無に応じてcorrectness questionを含めるdecision経路と、異なるquestion集合を識別できるdecision id/version・report metadata | 手段 | state検証後、provider request構築前にquestion集合を選択し、実行後のreportへ契約識別子を記録する | reference依存評価契約 | Reference依存評価契約とは、reference answerの有無に応じて選択されたquestion集合と、その集合を一意に示す識別情報を指す。 |
| 前回P1 component metrics指摘 | RAGのatomic judgment品質をretrievalとgenerationに分け、実際にそのevaluatorが返した値だけで測定できるか判定する | 各judgmentのground truth、evaluator固有prediction、accuracy、Brier、ECEを持つreportと回帰テスト | 記録 | 各evaluatorのcomponent probability取得後、最終diagnosis集計と並行して生成する | RAG component metrics | RAG component metricsとは、retrievalとgenerationの各atomic judgmentについて、そのevaluator自身のpredictionから算出したaccuracy・Brier・ECEを指す。 |
| ユーザーの対応完了報告 | Issue #5・#9の完了可否を再判定する | 前回5指摘の修正、追加テスト、offline report、package検査をコードと再実行結果から確認するレビュー所見 | 記録 | 対応表作成後、差分確認とコマンド再実行後に提示する | 再レビュー所見 | 再レビュー所見とは、前回指摘の解消状況と新規回帰の有無を重要度順に示す記録を指す。 |
