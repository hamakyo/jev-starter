| 出典 | 目的 | 具体対象 | 役割 | 前後関係 | 候補語 | 初出定義 |
| --- | --- | --- | --- | --- | --- | --- |
| P1 cascadeレビュー | fallbackへ進んだ1行の実行量と品質を正しく比較する | primary結果とfallback結果の予測、route、latency、usage、model、pricingを行単位で保持し、成功したlegだけを合算する集計 | 記録 | primary実行後、fallback実行時だけfallback legを追加し、end-to-end report生成前に合算する | cascade leg / end-to-end aggregation | Cascade legとは、同じfixture行に対するprimaryまたはfallbackの一回分の評価結果を指す。End-to-end aggregationとは、同じ行の実行済みlegを合算して最終的な品質・レイテンシ・usage・costを計算する処理を指す。 |
| P1 referenceレビュー | reference answerがない行でreference依存の判断を実行しない | referenceの有無、correctness質問を除いた質問集合、correctnessを除いたdiagnosis入力 | 開始条件 | JSONLを読み込んだ後、provider requestとdiagnosisを行う前に分岐する | reference-aware decision | Reference-aware decisionとは、reference answerが存在する行だけcorrectness質問とcorrectness診断を有効にするdecision経路を指す。 |
| P1 component metricsレビュー | RAGの各atomic judgmentの品質と検索・生成のまとまりを別々に測る | retrieval/generationのcomponent予測、component ground truth、accuracy/Brier/ECE、group report | 記録 | 同一fixtureの各行を評価した後、最終diagnosis metricsと並行してcomponent metricsを生成する | RAG component report | RAG component reportとは、retrievalとgenerationの各judgmentについてground truthと確率品質を保持し、グループ別に集計した記録を指す。 |
| P2 subset hashレビュー | fallback対象集合の再現性を元datasetの識別子と混同しない | parent dataset hash、fallback subsetの選択ID、subset raw representationのhash/count | 記録 | primary判定でfallback対象IDを確定した後、fallback reportを生成する | cascade selection | Cascade selectionとは、primary結果によってfallbackへ送ったfixture IDの順序付き集合を指す。 |
| P2 accuracyレビュー | provider failureを隠さず評価品質と到達率を読めるようにする | successful rowsのaccuracy、全dataset rowsを分母にした end-to-end accuracy、failure count、coverage | 値 | prediction結果をsuccess/failureに分けた後、reportとMarkdown表示で併記する | overall accuracy / successful accuracy | Overall accuracyとは全fixture行を分母にして失敗を不正解相当として扱う値を指す。Successful accuracyとは成功したpredictionだけを分母にする補助値を指す。 |
